// src/Results.js
import React from 'react';

const Results = () => {
    return (
        <div>
            <h2>Results Page</h2>
            {/* Display results here */}
            <p>This is where the prediction results will be shown.</p>
        </div>
    );
};

export default Results;
